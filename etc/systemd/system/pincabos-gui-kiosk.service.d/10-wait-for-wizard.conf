[Service]
# Let the Flask wizard answer before WebKit loads the URL, otherwise the kiosk
# shows a connection error on slower machines.
ExecStartPre=/bin/bash -c 'for i in $(seq 1 30); do (exec 3<>/dev/tcp/127.0.0.1/8046) 2>/dev/null && { exec 3>&-; exit 0; }; sleep 1; done'
