[Service]
# v5 fix: worker must NOT share the WebApp engine lock (export.lock self-deadlock:
# worker holds it while curl-ing the WebApp which tries to take it again)
Environment=PINCABOS_BATCH_LIVE_SHARED_LOCK=/var/lib/pincabos/batch-live/worker-guard.lock
