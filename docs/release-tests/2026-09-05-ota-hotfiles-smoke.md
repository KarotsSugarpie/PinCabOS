# OTA HotFiles smoke release — 2026-09-05

Purpose: trigger one normal PinCabOS beta release to validate the new transactional update flow end-to-end:

Backup HotFiles -> online update -> restore HotFiles -> final GO/NOGO -> explicit reboot choice.

This documentation marker is intentionally outside the updater-managed paths, so it is not deployed to cabinets and has no runtime effect.
